import { SortnessDirective } from './sortness.directive';

describe('SortnessDirective', () => {
  it('should create an instance', () => {
    const directive = new SortnessDirective();
    expect(directive).toBeTruthy();
  });
});
