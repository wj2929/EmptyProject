import { UniqueCustomFormItemNameValidator } from './unique-custom-form-item-name-validator';

describe('UniqueCustomFormItemNameValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueCustomFormItemNameValidator()).toBeTruthy();
  });
});
