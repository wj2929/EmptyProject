import { UniqueCustomFormItemKeyValidator } from './unique-custom-form-item-key-validator';

describe('UniqueCustomFormItemKeyValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueCustomFormItemKeyValidator()).toBeTruthy();
  });
});
