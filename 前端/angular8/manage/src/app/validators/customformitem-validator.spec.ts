import { CustomformitemValidator } from './customformitem-validator';

describe('CustomformitemValidators', () => {
  it('should create an instance', () => {
    expect(new CustomformitemValidator()).toBeTruthy();
  });
});
