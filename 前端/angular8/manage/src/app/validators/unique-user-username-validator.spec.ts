import { UniqueUserUsernameValidator } from './unique-user-username-validator';

describe('UniqueUserUsernameValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueUserUsernameValidator()).toBeTruthy();
  });
});
