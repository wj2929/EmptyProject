import { UniqueCustomFormKeyValidator } from './unique-custom-form-key-validator';

describe('UniqueCustomFormKeyValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueCustomFormKeyValidator()).toBeTruthy();
  });
});
