import { UniqueCategoryTypeKeyValidator } from './unique-category-type-key-validator';

describe('UniqueCategoryTypeKeyValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueCategoryTypeKeyValidator()).toBeTruthy();
  });
});
